{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); CMD=$(echo \"$INPUT\" | jq -r '.tool_input.command // \"\"'); if echo \"$CMD\" | grep -qE '(rm -rf /|DROP TABLE|DELETE FROM.*WHERE 1|shutdown|:(){ :|:& };:)'; then echo 'BLOQUEADO: Comando destructivo detectado' >&2; exit 2; fi; exit 0"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "notify-send 'Claude Code' 'Tarea completada' --icon=dialog-information 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
